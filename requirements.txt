# Python dependencies for the pipeline scripts.
#
#     python -m pip install -r requirements.txt
#
# openpyxl is the only third-party package. Everything else imported by the
# Python pipeline is part of the standard library; GARbro access and image
# rendering live in the compiled C# resource backend.
#
# The bound is a minor-version range, not a loose floor. openpyxl has changed
# how read_only worksheets report dimensions and how styles round-trip between
# minor releases, and this toolset reads workbooks a human has edited in Excel -
# exactly where those differences show. Tested against 3.1.5.
openpyxl>=3.1,<3.2
